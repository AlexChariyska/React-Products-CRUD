import React from 'react';

const Header = () => (
	<header>
		<h1 className="heading">Products Application</h1>
		<p className="sub-heading">Simple application using React that will be able to C, R, U, D product items.</p>
	</header>
);

export default Header;
